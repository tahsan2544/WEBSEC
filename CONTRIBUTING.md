# Contributing

1. Create a focused branch.
2. Make a small, testable change.
3. Run `python -m pytest -q`.
4. Update docs when behavior changes.
5. Keep checks read-only, deterministic, conservative, and actionable.

Patch releases increase by 0.0.1 with `python scripts/bump_version.py`.
