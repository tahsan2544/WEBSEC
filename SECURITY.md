# Security Policy

Use only for systems you own or have explicit permission to test.
The tool is intentionally read-only and does not perform brute force, credential attacks,
authentication bypass, exploit payloads, database writes, destructive requests, or credential collection.
